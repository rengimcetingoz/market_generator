from setuptools import setup

setup(
   name='gan_portfolios',
   version='1.1',
   description='...',
   author='CAL',
   author_email='CAL',
   packages=['gan_portfolios'],  #same as name
   install_requires=[], #external packages as dependencies
)
